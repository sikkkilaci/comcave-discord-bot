services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: 'file:/app/data/bot.db'
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
